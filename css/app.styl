@import utils

body > :not(header)
header > p
	display none

header h1
	color rgb(0, 0, 0)
	font-family LeagueGothicRegular, serif
	font-size 80px
	malarkey 347px 61px url(../img/logo.png)
	margin-top 50px
	text-align center
	text-transform uppercase
	margin 50px auto
